# 编译平台配置
# 设为 false 可跳过对应平台，加快编译速度
# 设为 true 启用编译

platforms:
  windows: true        # Windows x86_64 (MSI + EXE 安装包)
  windows_arm64: false # Windows ARM64 (需要 Flutter 3.44)
  linux: true          # Linux x86_64 + aarch64 (DEB + RPM)
  android: true        # Android aarch64 (APK)
